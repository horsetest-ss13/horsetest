/// Attempt to get the turf below the provided one according to Z traits and virtual levels
#define GET_TURF_BELOW(location) get_turf_below_helper(location)

/// Attempt to get the turf above the provided one according to Z traits and virtual levels
#define GET_TURF_ABOVE(location) get_turf_above_helper(location)
